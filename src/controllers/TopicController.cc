#include "TopicController.h"

// Add definition of your processing function here
void TopicController::search(const HttpRequestPtr &req, std::function<void(const HttpResponsePtr &)> &&callback) {
    // verify permission


}
